# Call Graph

Stub. Function-to-function and module-to-module call graphs aren't applicable yet — there's no executable code in this repo (see `CODE_EXPLANATIONS.md`).

The closest current analog is the skill-to-skill handoff map in `SKILLS_ARCHITECTURE.md` → "Handoff map", which documents which skills call/feed into which others. Once real code exists (voice pipeline, MCP wiring, HUD), `document-commit` should populate this with actual call relationships.
