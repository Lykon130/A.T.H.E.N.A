# Code Explanations

Stub. This repo currently contains architecture documentation and skill specifications (markdown), not executable code — no MCP wiring scripts, no voice pipeline code, no HUD implementation yet.

Once real code exists (Obsidian MCP wiring, faster-whisper/Piper glue scripts, the HUD, any git-hook tooling beyond the shell script in `.githooks/`), `document-commit` should populate this file — or split it into `docs/modules/{module}.md` per module if it grows large — with what each file/module does and why, keyed to the commits that introduced them.
