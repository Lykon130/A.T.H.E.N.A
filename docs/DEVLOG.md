# Development Log

Seeded from the full development history (conversation-driven — most of this predates git being wired in, so this is richer than `git log` alone would show). `document-commit` appends one entry per commit going forward.

**Origin.** Project started from a JARVIS OS concept (voice → Claude Code → Obsidian vault → HUD, 4-part build), sourced from a set of reference slides. Initial build guide drafted, tailored to a Windows + Fedora dual-machine setup with Claude Code already installed.

**Reference synthesis.** Reviewed several other AI-assistant/business-OS reference builds: "Optimal Engine" (brain-store knowledge graph + Conductor routing + skill maturity ladder), "AI Headquarters" (department-dashboard concept), sagar_builds' "Ultron" (edge compute / gesture control / call-UI voice), "Council of High Intelligence" (multi-perspective deliberation), "AI Content Team OS" (research→hooks→scripts→design→publish→analyze pipeline), "One Claude, 11 Departments, 92 Skills" (an official skill taxonomy), and two AI trading/investing concepts.

**Merge and naming.** Decided to merge all references into one project rather than treat them as separate ideas. Named it A.T.H.E.N.A — Autonomous Tactical Hub for Execution, Networking & Analysis. Key architectural calls made at this stage: Claude Code doubles as engine and Conductor (no separate router needed); departments are skill groups, not standing agents (rejected the "8 independent AI workers" model as over-engineered for a one-person build); the Obsidian vault is the "brain-store" for free, no separate graph database.

**Initial scaffold.** `ARCHITECTURE.md` written. Vault structure (`raw/wiki/outputs`) and initial skills built: `vault-write`, `vault-health`, `inbox`, `metrics`, `plan`, `trends`.

**Cost model decision.** Confirmed Claude Code is included in the Claude Pro subscription, sharing one usage pool with Claude.ai chat and Cowork. Established the standing design rule: Claude is only called for reasoning/synthesis; all mechanical work (data pulls, polling, scanning) happens in plain scripts first. This is what keeps the growing skill count sustainable inside a Pro plan rather than requiring a from-scratch local-model build.

**Department expansion, round 1.** Added Legal and HR departments (taxonomy only, HR left empty — no employees). Added `finance/research-score`, modeled on an AI-investing reference, with the first hard boundary established: research and scoring only, never trade execution.

**News.** Added a News department: `news/global-scan` (GDELT — free, unlimited, no key) and `finance/news-monitor` (Finnhub free tier) for watchlist-specific alerts. Same hard boundary extended: monitoring/alerting only.

**Department expansion, round 2 — four focus areas.** User specified the actual scope: personal ops, solo business/freelance, content creation, investing/trading research. Built accordingly: `strategy/deliberate` (the Council pattern, at this point still Strategy-scoped) and `operations/calendar` for personal ops; `sales/pipeline-review`, `sales/draft-outreach`, `support/client-response`, `legal/review-contract` for solo business (draft-only boundary established for outreach/responses); `marketing/research`→`hooks`→`scripts`→`analyze` for content; `finance/portfolio-track`, `finance/watchlist-scan` for investing.

**JARVIS completionism.** User asked for the full JARVIS/Ultron feature set: memory, booking, and the Council pattern (already partially built). Added `vault/remember` + `vault/wiki/profile.md` (persistent user profile), a new Concierge department with `book-appointment`, and generalized `deliberate` into a cross-cutting `council` skill usable by any department, not just Strategy. Established the booking hard boundary: research/draft only, submission always needs per-instance confirmation, never handles payment/ID data.

**Parallel direct edits.** Several additions landed directly in the vault outside the conversation flow, in the same established pattern: `data/report` (cross-department weekly rollup), `concierge/plan-travel`, `design/ideate` (new Design department), `vault/digest` (proactive flagging, distinct from `report`'s full rollup).

**Health and Engineering.** Resolved the two remaining ambiguous departments: Health built as `health/log-health`, manual-entry based (the user's Noise wearable has no direct API, and its Google Fit bridge is being deprecated in 2026 in favor of Health Connect, which isn't remotely queryable — live sync deferred). Engineering built as `engineering/dev-digest` (GitHub/GitLab, read-only). Home/Environment left an intentional stub — a bare smart TV with no hub/app ecosystem was judged too thin to justify a department.

**Documentation automation.** User requested an on-every-commit documentation skill: system architecture, SRS, technical docs, code explanations, skill/module architecture, function-to-function call graphs, and a full development log. Split into `engineering/docs-init` (one-time full baseline) and `engineering/document-commit` (incremental, diff-scoped updates) to avoid regenerating everything on every commit, which would be slow and would burn Claude Pro's shared usage pool on a busy dev day. Documented the trigger tradeoff (post-commit vs. pre-push) and the need for scoped tool permissions on unattended headless runs.

**This baseline.** Discovered the repo was already git-initialized with a remote (`github.com/Lykon130/A.T.H.E.N.A`) and one prior commit. Applied `docs-init` to A.T.H.E.N.A itself as the first real target, adapted to current reality: no executable code exists yet, so `CODE_EXPLANATIONS.md` and `CALL_GRAPH.md` are stubs rather than fabricated content, `SKILLS_ARCHITECTURE.md` documents the routing/handoff model that does exist, and `SRS.md` reconstructs requirements from every decision made so far rather than being written speculatively upfront. Wired `.githooks/post-commit` for `document-commit` going forward (not yet activated — needs `git config core.hooksPath .githooks` run locally).

- **798bcc0** (2026-08-07) — "Autonomous Tactical Hub for Execution, Networking & Analysis (A.T.H.E.N.A) — Research v1.0." Root commit: wrote `ARCHITECTURE.md` and added the full v1 skill/department scaffold in one pass — 23 skill files across 13 departments (Concierge, Council, Data, Design, Engineering, Finance, Health, Legal, Marketing, News, Operations, Sales, Strategy, Support, Vault) plus `vault/wiki/` org-chart, profile, and per-department notes. Content is already captured in `SKILLS_ARCHITECTURE.md`, `SRS.md`, and `TECHNICAL.md` via the later `docs-init` baseline; this entry backfills the commit-log record for it.
- **acedaa9** (2026-08-07) — "Add Engineering docs-init and document-commit skills." Added `skills/engineering/docs-init.md` (one-time full documentation baseline generator) and `skills/engineering/document-commit.md` (incremental, diff-scoped doc updater — the skill this very log entry was produced by), and updated `vault/wiki/departments/engineering.md` and `vault/wiki/org-chart.md` to list both new skills against the Engineering department. Content already captured in `SKILLS_ARCHITECTURE.md`, `SRS.md` (FR-15), `TECHNICAL.md`, `README.md`, and `CALL_GRAPH.md` via the later `docs-init` baseline, which was generated from a tree that already included this commit; no further patching needed there.
- **2c52a69** (2026-08-07) — "docs: initial documentation baseline." Ran `docs-init` against the repo as it stood at that point: created `docs/README.md`, `docs/SRS.md`, `docs/TECHNICAL.md`, `docs/SKILLS_ARCHITECTURE.md`, `docs/DEVLOG.md` (seeded from the full build history), and stub `docs/CALL_GRAPH.md`/`docs/CODE_EXPLANATIONS.md` (no executable code exists yet). Also updated root `ARCHITECTURE.md` to reflect the current department/skill set and cross-cutting skills. This is the commit that produced the doc set itself, so its content is already fully reflected in every file above — no further patching needed here; this entry only backfills the commit-log record.
- **005552d** (2026-08-07) — "engineering: wire document-commit as a pre-push hook." Settled the post-commit vs. pre-push tradeoff in favor of pre-push: added `.githooks/pre-push`, which handles the real pre-push stdin protocol (multiple refs, new-branch and delete-branch cases) and loops over every commit in the push range that touches something outside `docs/`, so it never re-documents its own doc-update commits; a failed doc run warns but never blocks the push. Updated `skills/engineering/document-commit.md` to describe the pre-push trigger and the scoped headless tool permissions instead of post-commit. `docs/TECHNICAL.md` and `docs/SRS.md` already reflected pre-push (the 2c52a69 baseline was generated from a tree that already included this commit); this entry backfills the commit-log record and removes the now-stale "not yet resolved" line in `SRS.md` that still claimed post-commit was current.
- **a9884ca** (2026-08-07) — "engineering: scaffold vault raw/outputs folders, wire Obsidian MCP connection." Closed the standing Step 4 blocker from `ARCHITECTURE.md`: scaffolded `vault/raw/<department>/` and `vault/outputs/<department>/` alongside the existing `wiki/`; installed Obsidian and Syncthing (via `winget`); enabled the Local REST API plugin (the installed version bundles a native Streamable HTTP MCP endpoint at `/mcp`, so no separate `mcp-obsidian` npm wrapper was needed); worked around the plugin's self-signed HTTPS cert by enabling its plain-HTTP port (27123, loopback-only) rather than fighting TLS trust; wired `.mcp.json` (gitignored — holds the API key) to that endpoint and verified the connection with a live write/read/delete round-trip. Added `.mcp.json.example` to document the connection shape, and gitignored `vault/.obsidian/` and `vault/.stfolder/` since both contain machine-local secrets/state (the Local REST API plugin stores its API key and TLS private key in `vault/.obsidian/plugins/obsidian-local-rest-api/data.json`). Registered a Syncthing folder for the vault and a logon-time scheduled task so sync survives reboots; Fedora-side device pairing is still pending (needs that machine's device ID). The `document-commit` run for this commit hit its max-turn limit mid-edit — this entry and the `TECHNICAL.md` status-table fix were completed manually to finish what it started.
- **b096e38** (2026-08-10) — "engineering: build local voice module (wake word + speaker verification + STT/TTS)." Closed `ARCHITECTURE.md` build-order step 5, the first real executable code in this repo. Built `voice/`: openWakeWord for wake-word activation (interim `hey_jarvis` model — custom "Hey Athena" training is GPU-oriented and deferred), local SpeechBrain ECAPA-TDNN speaker verification gating every command before it reaches Claude, faster-whisper for STT, Piper for TTS, and a headless `claude -p` bridge resuming a dedicated session so voice turns keep context; transcripts log to `vault/raw/engineering/voice-sessions/`. Added `skills/engineering/voice-setup.md` for repeatable per-machine setup (Windows and Fedora). Live testing on Windows caught two real bugs and fixed both: the original resemblyzer/GE2E-based verification produced a false accept (an impostor scored *higher* similarity than the genuine owner), replaced with ECAPA-TDNN (confirmed via live test: 0.73 for the owner, 0.26 for an impostor); and Claude's markdown-formatted replies made Piper spell out symbols like `**` and `D:\` aloud, fixed by a `strip_markdown` pass before TTS. Updated `docs/CODE_EXPLANATIONS.md` and `docs/CALL_GRAPH.md` from stubs to real content (first executable code), and `docs/SRS.md`/`docs/TECHNICAL.md` to reflect voice as built rather than planned.
- **87c5d77** (2026-08-10) — "engineering: build A.T.H.E.N.A desktop HUD (Electron + neural-mesh idle view, command bar, per-department detail UIs)." Closed `ARCHITECTURE.md` build-order step 6. Built `hud/`: Electron + React + TypeScript, polling the vault via the Obsidian Local REST API plugin (same `.mcp.json` credentials as Claude Code's own MCP connection). Idle view is a rotating 4-layer neural mesh (Fibonacci-sphere nodes, k-NN + cross-layer bridge edges) with department zones that glow on real vault activity and flash on command, plus a speaking-reactive wave sweep (synthetic envelope for now — real voice amplitude wiring deferred). Departments only surface on command: Ctrl+K opens a command palette (`lib/commands.ts`'s `parseCommand` kept as a standalone pure function so a future voice-command path can reuse it) revealing the department orbit or jumping straight to a detail view. Detail views are archetype-based (`MetricsPanel`, `FeedLogPanel`, `PipelinePanel`, `ReferencePanel`) rather than one generic template, assigned per department by how it actually surfaces data; metrics render honest empty states rather than fabricated numbers. Also fixed two stale `ARCHITECTURE.md` lines (step 4/6 status, commit count) left over from the previous session. Updated `docs/CODE_EXPLANATIONS.md` and `docs/CALL_GRAPH.md` with the new `hud/` module, and `docs/SRS.md`/`docs/TECHNICAL.md` to reflect the HUD as built rather than planned.
- **4a9da96** (2026-08-11) — "engineering: fix document-commit's ARCHITECTURE.md blind spot, refresh build-order docs." `document-commit`'s routing table and its pre-push prompt both pointed at a nonexistent `docs/ARCHITECTURE.md`, so the canonical root-level `ARCHITECTURE.md` had never actually been reachable by an automated doc-update commit since the hook was wired up. Widened the hook's write scope (skill routing table, headless prompt, `--allowedTools`) to include root-level `ARCHITECTURE.md` alongside `docs/`. Also caught `ARCHITECTURE.md`'s own build-order checklist up to reality: steps 6 (HUD) and 7 (activate the pre-push hook) were already done but still marked otherwise — both content changes landed directly in `ARCHITECTURE.md` as part of this commit, so no further patching was needed there. Updated `docs/TECHNICAL.md` (Git hooks section, write-scope note) and `docs/SKILLS_ARCHITECTURE.md` (handoff map, routing-table clarification) to reflect the fix.
- **b75c93a** (2026-08-11) — "engineering: wire live voice activity into the HUD idle view." Closed the SRS §6 open requirement on the idle view's speaking-reactive wave sweep, which had used a synthetic sine-blend envelope since the HUD was first built. Added `hud/src/main/voiceBridge.ts`, hosting a local WebSocket server (`127.0.0.1:8765`) in the Electron main process, and `voice/hud_bridge.py`, a best-effort client `listen.py` now threads through every `speak()` call. While Piper's audio plays via `sd.play()`, `tts.py` samples real RMS amplitude off the actual playback buffer (~30Hz) and streams it to the HUD, which relays it to the renderer over IPC; `NeuralIdle.tsx`'s wave envelope now reads that live, exponentially-smoothed signal. Both sides degrade gracefully on their own: `voice/` no-ops with no HUD running, and the HUD sends a synthetic idle frame if the voice process disconnects mid-utterance. The old synthetic envelope moved to `lib/syntheticVoice.ts`, kept as the engine behind the manual Ctrl+K "speak" command-bar test. Verified live — launched the HUD, streamed simulated amplitude over the real socket, and confirmed via screenshots that the wave reacts and settles back to idle. The `document-commit` run for this commit hit its max-turn limit mid-edit (same as `a9884ca`) after already patching `ARCHITECTURE.md` and `docs/SRS.md` correctly; this entry plus the remaining `docs/CODE_EXPLANATIONS.md`, `docs/CALL_GRAPH.md`, and `docs/TECHNICAL.md` updates were completed manually to finish what it started.
- **efe3dbe** (2026-08-11) — "engineering: fix pre-push hook re-documenting its own ARCHITECTURE.md-touching doc commits." Widening `document-commit`'s write scope to include root-level `ARCHITECTURE.md` (`4a9da96`) missed one spot: the pre-push hook's own "skip my own docs commits" filter still only excluded `docs/`, so a doc-update commit that also touched `ARCHITECTURE.md` would get flagged as non-doc and re-trigger the hook on itself. Excluded `ARCHITECTURE.md` from that filter too (`.githooks/pre-push`'s `git rev-list` now passes `':!docs' ':!ARCHITECTURE.md'`). `docs/TECHNICAL.md`'s Git-hooks section already reflected the fix as part of this same commit, so no further patching was needed there; no other doc referenced the hook's internal filter logic.
- **8201170** (2026-08-11) — "engineering: recolor the HUD idle sphere as a cosmic composite." Purely visual pass over `hud/`'s idle view: swapped the single green `--accent` (`theme.css`) for a violet accent across the UI chrome, and gave `NeuralIdle.tsx`'s neural mesh a fifth shell so its five concentric layers each read as a distinct celestial body (magenta-violet nebula core, gold star cluster, red giants, a blue shell, a green outer shell) instead of one hue fading by alpha with distance. Added `blendRgb` so cross-layer bridge/long-range edges blend the two colors they connect, and `lightenRgb` so traveling spark particles tint to match their edge rather than always rendering near-white. Also raised brightness floors (intensity, alpha, line width) on every layer so the outer shells stay visible against the black background. Updated `docs/CODE_EXPLANATIONS.md`'s `NeuralIdle.tsx` row (4-layer -> 5-layer, added the per-layer color/blend description); no other doc referenced layer count, accent color, or shell count.
